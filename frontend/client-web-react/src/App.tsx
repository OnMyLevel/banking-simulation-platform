export function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Banking Simulation Platform</p>
        <h1>Client banking portal</h1>
        <p>
          Frontend foundation is ready. The next steps can add authenticated screens,
          account views and operation forms on top of the Gateway API client.
        </p>
      </section>
    </main>
  );
}
